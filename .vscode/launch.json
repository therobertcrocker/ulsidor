{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "console": "integratedTerminal",
            "program": "${workspaceFolder}/cmd/ulsidor/main.go",
            "args": ["quests", "create"],
            "cwd": "${workspaceFolder}",  // This ensures the working directory is the root of your project
            "debugAdapter": "dlv-dap",
        
        }
    ]
}